SELECT Users.full_name,Feedback.rating,Feedback.comments,Events.title FROM Users INNER JOIN Feedback ON Users.user_id = Feedback.user_id  INNER JOIN Events ON Feedback.event_id = Events.event_id WHERE Feedback.rating < 3;